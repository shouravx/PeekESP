{
  "name": "peek-relay",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Cloudflare Worker relay so PeekESP works without an inbound port",
  "scripts": {
    "test": "node test/worker.test.mjs",
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  }
}
